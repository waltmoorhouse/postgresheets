// LEGACY WRAPPER (NEUTRALIZED)
//
// The real mock is `vscode.ts`. This wrapper is preserved as a no-op
// shim to avoid unexpectedly triggering legacy mock behavior while
// keeping repository history intact.
module.exports = {};
